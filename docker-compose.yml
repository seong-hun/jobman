services:
  scheduler:
    build:
      context: ./
      dockerfile: docker/scheduler.Dockerfile
    ports:
      - "8000:8000"

  agent1:
    build:
      context: ./
      dockerfile: docker/agent.Dockerfile
    ports:
      - "8001:5000"

  agent2:
    build:
      context: ./
      dockerfile: docker/agent.Dockerfile
    ports:
      - "8002:5000"

  agent3:
    build:
      context: ./
      dockerfile: docker/agent.Dockerfile
    ports:
      - "8003:5000"
